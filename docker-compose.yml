services:
  api:
    build: .
    env_file: .env
    ports:
      - "8240:8000"
    volumes:
      - ./data:/app/data

  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    environment:
      ABTESTLAB_API_URL: http://api:8000
    ports:
      - "8741:8501"
    depends_on:
      - api
